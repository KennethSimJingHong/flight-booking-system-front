export class Passenger{
    firstName: string;
    lastName: string;
    identityNo: string;

    constructor(firstName: string, lastName: string, identityNo: string){
        this.firstName = firstName;
        this.lastName = lastName;
        this.identityNo = identityNo;
    }
}